# Plan2Space — execution rulings and deferred items

Every deviation from `2026-09-23-plan2space-implementation.md` made while executing it, in order, each with
the finding, the decision and (where recorded) what it costs if wrong. Exported from the execution ledger.

## Rulings (82)

- Task 1: Ruling: minio/minio is gone from Docker Hub (pull access denied) — pinned quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z — cost if wrong: one image line; MinIO upstream no longer ships community images, long-term may need a replacement
- Task 2: Ruling: plan never creates .sln/.csproj — scaffolded Plan2Space.sln + Domain/Application/Infrastructure/API (net8.0) + Application.Tests (xUnit); EF Core/Npgsql 8.0.11, NTS 2.5.0 — cost if wrong: version bumps
- Task 2: Ruling: host 5432 is a native Postgres and [::1]:5433 is held by wslrelay — override maps compose Postgres to host 5433, tests/design-time use Host=127.0.0.1;Port=5433 — cost if wrong: connection strings in later test files must follow this
- Task 2: Ruling: SchemaTests email made unique per run (unique index on Users.Email + persistent test DB made the brief's fixed email fail on re-run) — cost if wrong: none
- Task 2: Ruling: added DesignTimeDbContextFactory in Infrastructure so `dotnet ef` works before Task 3 wires DI; migrations in Persistence/Migrations via --output-dir — cost if wrong: one extra file
- Task 3: Ruling: brief's handlers import Plan2Space.Infrastructure.Persistence from Application (circular: Infrastructure→Application) — added Application/Common/IPlan2SpaceDbContext implemented by Plan2SpaceDbContext; ALL later handlers inject IPlan2SpaceDbContext instead of Plan2SpaceDbContext — cost if wrong: mechanical type swap
- Task 3: Ruling: `using BCrypt.Net; BCrypt.HashPassword` binds to the namespace — use BCrypt.Net.BCrypt.* fully qualified — cost: none
- Task 3: Ruling: JwtBearer MapInboundClaims=false + RoleClaimType="role" so Task 4's User.FindFirst("sub") works (default maps sub→nameidentifier) — cost if wrong: claim lookups
- Task 3: Ruling: brief left duplicate-email/wrong-password as unhandled exceptions (500) — controller maps them to 409/401, with tests Register_DuplicateEmail_Returns409 and Login_WithWrongPassword_Returns401 (spec auth table) — cost if wrong: status codes
- Task 3: Ruling: Plan2SpaceWebApplicationFactory uses Testcontainers.PostgreSql 4.1.0 (postgis/postgis:16-3.4-alpine), config via UseSetting, migrates in InitializeAsync
- Task 4: Ruling: added tests OtherUser_CannotSeeOrDeleteProject + Unauthenticated_Returns401 (owner scoping is the task's security claim); RegisterAndLoginAsync lives in AuthTestHelpers.cs as an extension on the factory — cost: none
- Task 5: Ruling: brief's conflict check used max(row.Version) — saving an empty plan reset version to 0 and two concurrent saves could both pass. Added Project.GeometryVersion (uint, IsConcurrencyToken, migration ProjectGeometryVersion); save requires BaseVersion == GeometryVersion exactly, DbUpdateConcurrencyException → 409. Test SavingEmptyGeometry_DoesNotResetVersion — cost if wrong: one column
- Task 5: Ruling: GeometryDto gained trailing `Version` (project geometry version) so clients know what BaseVersion to send — additive; Task 16/21 TS types must include it — cost if wrong: one field
- Task 5: Ruling: brief deleted+recreated walls with fresh Guids each save, orphaning every Opening.WallId. WallInput/RoomInput gained optional `Id` (upsert by id); opening with unknown WallId → 400. Tests OpeningKeepsPointingAtItsWall_AcrossSaves, OpeningOnUnknownWall_Returns400. Task 12 serializer / Task 21 client should send ids — cost if wrong: optional field, backward-compatible
- Task 5: Ruling: overlap → 422 {message, overlaps[{roomAId,roomBId}]} (brief threw InvalidOperationException → 500); invalid room (<3 pts, self-intersecting) → 400, unclosed outline auto-closed; missing/foreign project → 404 — tests OverlappingRooms_Returns422_AndNothingIsStored, SaveOnSomeoneElsesProject_Returns404 — cost if wrong: status codes
- Task 6: Ruling: spec names (POST /api/projects/{id}/upload, WS /ws/jobs/{id}, channel job:{id}:events, 50MB) differ from plan (…/files, /ws/job/{id}, job:{id}:updates + key job:{id}:progress, 60MB global constraint) — kept PLAN names because Tasks 9/12/16 consume them — cost if wrong: route/channel rename across services
- Task 6: Ruling: brief omitted the upload endpoint code — added FilesController + UploadProjectFileCommand (owner check, ext whitelist png/jpg/pdf/dxf/dwg, magic-byte check per spec security table, 60MB, key projects/{projectId}/{fileId}{ext}, bucket plan2space-uploads) + IFileStorage/MinioFileStorage (Minio 6.0.3); test Upload_RejectsFileWhoseBytesDoNotMatchItsExtension
- Task 6: Ruling: brief had no owner checks on enqueue/status/ws — all scoped to project owner (404 otherwise); WS requires JWT via ?access_token= (browsers can't set headers) — test OtherUser_CannotEnqueueOrReadJob. Task 16 web client must append ?access_token= — cost if wrong: WS auth wiring
- Task 6: Ruling: brief's status endpoint read only the DB row that workers never update — GetJobStatusQuery prefers Redis live state, DB fallback; test JobStatus_ReflectsWorkerProgressFromRedis
- Task 6: Ruling: brief's hub subscribed after catch-up (lost updates), hung forever on finished jobs / client disconnect, leaked subscriptions, sent concurrently — hub subscribes first, closes on terminal state, watches client close, unsubscribes in finally, serializes sends; test WebSocket_ReceivesLiveUpdates_AndClosesWhenJobCompletes
- Task 6: Ruling: enqueue marks job Failed if the RabbitMQ publish throws (no forever-Queued jobs); publisher uses a lock (IModel not thread-safe) and RabbitMQ.Client 6.8.1 (brief's IModel API); Redis config accepts redis:// URLs
- Task 6: Ruling: test factory now shares one set of Testcontainers (PostGIS/Redis/RabbitMQ/MinIO quay image) across classes, migration once under a lock; added test EnqueueJob_PublishesMessageInTheShapeTheWorkerReads pinning {job_id, project_id, file_object_key}
- Task 6: Ruling: Phase-1 checkpoint needs a runnable api image — compose api build context ./backend, Dockerfile copies src/ (no `|| mkdir` fallback that produced an empty image), ASPNETCORE_HTTP_PORTS=5000, Database__MigrateOnStartup, Minio__AccessKey/SecretKey env; postgres healthcheck now TCP (`-h 127.0.0.1 -d db`) because socket pg_isready passes during postgis init restart (root cause of the Task 2 flake). scripts/test-api-container.sh → PASS
- Task 7: Ruling: added workers/raw_consumer.py (Celery ConsumerStep on ai.vectorize.jobs → vectorize_job.apply_async on queue ai.vectorize.run); task route moved to ai.vectorize.run so Celery never writes into the raw queue — tests test_raw_job_consumer.py (5) + scripts/test-ai-queue-bridge.sh (real RabbitMQ, PASS) — cost if wrong: one module
- Task 7: Ruling: torch/torchvision pinned to +cpu via --extra-index-url (default Linux wheels add ~3GB CUDA); image is 3.62GB — cost if wrong: GPU inference needs a CUDA variant image later
- Task 7: Ruling: compose — redis and minio joined p2s_internal (workers need Celery backend/progress + source files; internal net still has no egress); ai + celery_worker share image tag plan2space-ai; pytest.ini pythonpath=. ; package __init__.py files added
- Task 8: Ruling: brief read only LWPOLYLINE — the project's own mvp_demo/sample_house_plan.dxf draws walls as LINE (4 LINE + 2 LWPOLYLINE on "WALL") and would yield zero walls. Parser reads LINE/LWPOLYLINE/2D POLYLINE (closed polylines closed); sample copied as fixture, test asserts 6 walls
- Task 8: Ruling: brief composed nested INSERT matrices as parent@insert (wrong for row-vector ezdxf) and sliced Vec3 (`[:2]` raises TypeError) — uses insert@parent and v.x/v.y; test test_nested_inserts_apply_inner_transform_before_outer
- Task 8: Ruling: block content on layer "0" inherits the INSERT's layer; "TUONG" added to wall-layer hints; $INSUNITS converted to metres (mm/cm/in/ft/dm; unitless = m); coordinates rounded to 1e-6 m — tests for each
- Task 8: Ruling: zero walls → NoWallsFoundError naming the drawing's layers (Review Focus: never silently produce zero walls); Task 12 must turn it into a Failed job with that message — cost if wrong: one exception type
- Task 9: Ruling: brief's bowtie test is vacuous (a self-intersecting shapely LineString is still is_valid) — kept it, and added test_self_intersecting_room_outline_is_repaired_to_a_valid_polygon: close_loops outlines are make_valid-repaired to the largest valid polygon; degenerate polylines dropped (output may be shorter than input) — cost if wrong: callers that index result by input position
- Task 9: Ruling: brief's skeleton→findContours traced both sides of every 1px line (each wall twice, interior walls overlapping) — contours are split into straight segments and collinear/overlapping ones merged (3px, 5°), segments <5px dropped. Tests: rectangle → exactly 4 walls ~118px; rectangle+divider → exactly 5
- Task 9: Ruling: brief's ViT loaded facebookresearch/dino via torch.hub (runtime GitHub download — forbidden on p2s_internal; DINO outputs embeddings not a heatmap; 1-channel input would crash). Replaced with torchvision vit_b_16(weights=None) + per-patch linear head → 14x14 logits upsampled to HxW; weights only from local checkpoint (arg or P2S_VIT_CHECKPOINT). use_model without checkpoint degrades to threshold with a warning. NO TRAINED CHECKPOINT EXISTS — real raster accuracy depends on fine-tuning (plan's Task 10 mentions a training script; not in any task's steps) — cost if wrong: model class swap
- Task 10: Ruling: brief snapped pairs to midpoints using coordinates captured before any move — 3 walls meeting at a corner end at different points. Now union-find clusters within tolerance → centroid; lone endpoints within tolerance of another wall's interior are projected onto it (T-junction, docstring's "closes small gaps"); input not mutated. Tests: three-endpoint corner, T-junction, no mutation, GNN output shape — cost if wrong: none (same signature/shape)
- Task 11: Ruling: brief's tests only exercised use_model=False (always []), so the OBB mapping was untested and had a bug — xywhr rotation is radians but was stored as angle_deg. Extracted symbols_from_obb (degrees, class names from model.names, non door/window skipped) with fake-box tests — cost if wrong: none
- Task 11: Ruling: missing YOLO weights (none exist yet) → warning + [] instead of YOLO() raising/attempting a download and failing the whole job; weights path via P2S_YOLO_WEIGHTS. NO TRAINED YOLO-OBB WEIGHTS EXIST — raster jobs currently produce no openings — cost if wrong: openings silently absent (logged)
- Task 11: Ruling: PaddleOCR downloads models on first use (impossible on p2s_internal) — Dockerfile bakes them at build (HOME=/opt/p2s-home owned by p2s); real-OCR test test_real_ocr_reads_a_dimension_label_and_ignores_plain_words + scripts/test-ai-offline-ocr.sh (docker --network none) PASS
- Task 11: Ruling: on Linux, importing pyclipper 1.4.0 or scipy AFTER paddlepaddle 2.6.2 aborts ("free(): invalid pointer" in inflateReset2 / zlib stream error) — root-caused by bisecting imports; fix = import cv2/pyclipper/scipy/shapely/skimage before paddleocr (in ocr_dimensions.py and the Dockerfile bake step) — cost if wrong: any new module importing paddle first reintroduces the crash
- Task 12: Ruling: brief's serializer emitted points [[x,y]] and wallIndex; Task 5's API binds points [{x,y}] and OpeningInput.WallId (Guid) → 400. Serializer now gives each wall a uuid "id", openings reference wallId, positions projected onto the wall, symbols >0.5m from any wall dropped — tests test_serializer.py
- Task 12: Ruling: brief pushed raster walls in PIXELS as metres ("calibration is Phase 4") though Task 11's interface says OCR dims give the px→m scale. Added pipeline/scale.py: parse labels (3000 / 3,000 / 4500mm / 3.6m), median(label / nearest-wall length), y-flip; fallback P2S_DEFAULT_METRES_PER_PIXEL=0.02 with warning; healing now runs in metres (0.05). test_raster_walls_are_scaled_from_pixels_to_metres — cost if wrong: raster plans without labels get an assumed scale
- Task 12: Ruling: worker has no user JWT and brief hard-coded baseVersion 0 (409 on any project saved before). Added InternalGeometryController (GET /internal/projects/{id}/geometry/version, PUT …/geometry) guarded by [InternalServiceToken] (X-Internal-Token == Internal:ServiceToken, fixed-time compare, fails closed), acting as the project owner; worker reads version then PUTs, retries once on 409. nginx doesn't proxy /internal/. Tests InternalGeometryTests (3), test_api_client.py (2) — cost if wrong: service-auth scheme
- Task 12: Ruling: task catches every failure → report_error (job:{id}:error) + "Failed|pct"; status API returns `error`. Tests test_pipeline_error_marks_job_failed_and_pushes_nothing, FailedJob_StatusCarriesTheWorkerErrorMessage
- Task 12: Ruling: global constraint "PDF rasterized page 1" — pypdfium2 4.30.0 added (pipeline/raster_input.py); "DWG converted via ODA or rejected" — no ODA, so API upload rejects .dwg with a message to export DXF (Upload_RejectsDwgUntilServerSideConversionExists)
- Task 12: Ruling: Task 7's contract test ran the task unpatched — now patches the I/O seams (task does real MinIO/Redis/API I/O since Task 12)
- Task 12: Ruling: compose — api gets Internal__ServiceToken; ai/worker get MINIO_ROOT_USER/PASSWORD, API_INTERNAL_URL, P2S_INTERNAL_TOKEN; .env.example P2S_INTERNAL_TOKEN. scripts/test-phase2-e2e.sh (full Docker: DXF→6 walls, PNG→4 walls) PASS; first run hit a transient RabbitMQ exit(1) during the concurrent 3.6GB image build, clean rerun passed
- Task 17: Ruling: konva on Node/jsdom resolves main index-node.js requiring native canvas — added exact alias `{ find: /^konva$/, replacement: 'konva/lib/index.js' }` in vite.config.ts and 2D canvas context mock in tests/setup.ts — cost if wrong: test-only mock
- Phase3-fix: Ruling: 3D built only points[0..1] of polyline walls (sample DXF has 5-point walls; L wall probe 5m instead of 5×4) — per-segment boxes merged; opening cutters oriented along their own segment (d9be268) — tests wallGeometryPolyline.test.ts
- Phase3-fix: Ruling: Scene re-ran CSG for every wall on every render, never disposed — useWallGeometry memoizes per wall + own openings and disposes (d9be268) — tests useWallGeometry.test.ts
- Phase3-fix: Ruling: 2D drew y-up plan data on y-down Konva (mirrored vs drawing/3D) — canvasTransform toScreen/screenDeltaToPlan, opening symbols follow their wall (39817f6) — tests canvasOrientation.test.tsx; follow-up fitView frames the plan on load/reset (8c8bd16) — tests fitView.test.ts
- Phase3-fix: Ruling: token only in memory (F5 = logout) and expired token made Dashboard silently empty — zustand persist (localStorage "p2s-auth"), 401 interceptor → logout, Dashboard error alert; removed DWG claim and fake v1.0 tag (3c91f3f) — tests authSession.test.tsx. Cost if wrong: token in localStorage is readable by XSS (standard SPA trade-off; no refresh endpoint exists)
- Phase3-fix: Ruling: no upload/"run AI" UI anywhere (plan gap, checkpoint unreachable) and job WS lacked ?access_token= (Task 6 requires it) — toolbar "Import plan" → runVectorization (upload, enqueue, WS progress, poll fallback on socket drop, error reason on Failed) → reload geometry (9913d3c) — tests aiImport.test.tsx
- Phase3-fix: Ruling: web image never built (nginx-spa.conf missing since Task 1) — added it (:3000, SPA fallback) + web .dockerignore (f8ff05f) — scripts/test-web-stack.sh PASS
- Task 22: Ruling: spec/plan want the ai-service to call GPT-4o mini (or Gemini Flash) but ai sat only on internal:true p2s_internal (no egress). Added network p2s_egress for `ai` ONLY (no published ports, not on p2s_public); celery_worker stays isolated — scripts/test-copilot-stack.sh asserts ai has egress, celery_worker has none — cost if wrong: ai container can make outbound connections
- Task 22: Ruling: LLM via OpenAI-compatible client (COPILOT_MODEL default gpt-4o-mini, COPILOT_BASE_URL, COPILOT_API_KEY/OPENAI_API_KEY), JSON mode; off-contract output → unknown; no key → "not configured"; provider errors (quota/auth/network) → unknown + "unavailable" reason instead of HTTP 500 (found live: host OPENAI_API_KEY had no credits). openai==1.51.0 added
- Task 22: Ruling: brief's handler sent rooms/openings as empty lists (wiping them on every co-pilot edit), dropped wall ids, used max wall version as BaseVersion, and .First() on an LLM-supplied id (500). Handler now edits the full plan with ids, moves a wall's openings with it, uses geometry.Version, validates ids/ranges → 422 with reason, AI unreachable → 503. Implemented add_opening (offset_m along the wall polyline, width, sill) and resize_room (scale about vertex centroid) which the brief left as placeholders. /copilot/parse on ai requires X-Internal-Token
- Task 22: Ruling: CopilotChat mounted as a Studio side panel (brief created it but never mounted it); response carries a human `message`
- Task 22: Ruling: found while testing — SaveGeometryCommand returned 500 when a request reused a wall/room id owned by ANOTHER project (PK collision). Such ids (and duplicates within a request) now get fresh ids; openings remapped to follow their wall. Test ReusingAnElementIdFromAnotherProject_SavesWithFreshIdsAndKeepsOpeningsAttached
- Task 23: Ruling: brief's StagingController called ai /staging/suggest but no task created that FastAPI route, and used an unregistered named HttpClient "AiInternal" without the service token — added api/routers/staging.py (X-Internal-Token, ValueError → 422) and IStagingClient/StagingHttpClient typed client (shared ConfigureAiClient with the co-pilot); controller validates polygon (400), AI 422 → 400 with reason, unreachable → 503
- Task 23: Ruling: exact label lookup sent "Bedroom 2"/"Phòng ngủ" to the living-room catalog — keyword match on accent-stripped label (bed/ngu → Bedroom; living/lounge/khach → Living Room; default Living Room); invalid/degenerate polygons raise ValueError. Tests: L-shaped room (polygon containment, not bbox), too-small room → [], label cases, endpoint token/422
- Task 24: Ruling: brief's export crashed for gltf (trimesh returns a dict of .gltf + .bin, `.encode` on dict) — gltf is now one self-contained file (embed_buffers) and glb (single binary, spec "glTF/GLB") was added; obj as before
- Task 24: Ruling: brief built each wall from points[0]→points[-1] (polyline walls became diagonals, same bug as the Phase-3 viewer) and ignored openings — per-segment boxes unioned and doors/windows boolean-cut with manifold3d==2.5.1 (added to requirements), heights matching the viewer (door 2.1, window 1.2). Accepts the API's {x,y} point objects
- Task 24: Ruling: project space is Z-up, glTF/OBJ are Y-up — export rotates -90° about X (house no longer lies on its side). Test test_glb_is_a_single_binary_file_and_y_is_up
- Task 24: Ruling: brief recorded an Asset3D row whose MinIO key was never uploaded — handler now PUTs the bytes to MinIO (exports/{projectId}/{assetId}.{ext}) before saving the row; test reads the object back. Format validated (400) before calling the ai-service; empty plan → 422; ai /export requires X-Internal-Token; typed IAiExportClient (shared ConfigureAiClient). Download file name = slug of the project name
- Task 25: Ruling: ifcopenshell==0.7.0 does not exist on PyPI (0.7.10+, 0.8.x) — pinned 0.8.0 (API differs: products=[...], void.add_opening/add_filling); reportlab==4.2.2
- Task 25: Ruling: brief's IFC created IfcWall entities with no representation, units or context (viewers show nothing) — IFC4 with SI units (m, m², m³, rad), Model/Body context, site/building/storey, one IfcWall solid per centreline segment, IfcOpeningElement voids + IfcDoor/IfcWindow fillings. Tests use ifcopenshell.geom.iterator (create_shape returned empty shapes intermittently in 0.8.0/Windows — the file itself verified correct)
- Task 25: Ruling: brief's BOQ used points[0]→points[-1] (polyline walls undercounted), ignored openings and used 60 bricks/m² for any thickness — full polyline length, door/window areas deducted, bricks & mortar scaled by thickness/0.1 m (Vietnam: ~60 bricks/m² per 110 mm leaf)
- Task 25: Ruling: spec's PDF = "BOQ + floor plan image + room schedule"; brief printed 5 text lines in Helvetica (no Vietnamese glyphs) — PDF draws the plan (walls to scale, rooms + labels), room schedule (area, perimeter) and BOQ, with DejaVu Sans from matplotlib (Helvetica fallback)
- Task 25: Ruling: .NET ExportFormats renamed Mesh→Supported and gained ifc (application/x-step) and pdf; Studio toolbar Export menu (GLB/glTF/OBJ/IFC/PDF, filename from Content-Disposition, API error message read from the blob) — plan had no export UI although the spec lists an Export module
- Task 26: Ruling: brief's single fixed-window "ai-triggering" limiter was GLOBAL (all users share 10/min; one user locks everyone out) and only covered vectorize+copilot. Spec (binding) says "100 req/min per user, 5 AI jobs/hour" and the global constraint says "rate limiting on all public and AI-triggering endpoints" — partitioned limiters keyed by JWT sub (client IP when anonymous): global 100/min (skips /internal worker calls), ai-jobs 5/hour on vectorize, ai-triggering 10/min on copilot/staging/export (plan's number), auth 10/min per IP on login/register; all configurable (RateLimiting:*); test factory raises them, RateLimitingTests re-applies spec values (5 tests incl. per-user isolation and brute-force)
- Task 26: Ruling: behind nginx every client had nginx's IP (one shared anonymous bucket) — nginx sends X-Forwarded-For/Proto, API UseForwardedHeaders trusting only 10/8, 172.16/12, 192.168/16, 127/8; spoofed XFF can't buy budget (scripts/test-security-stack.sh). Added X-Content-Type-Options/X-Frame-Options/Referrer-Policy headers at nginx
- Task 26: Ruling: no way existed to create the first admin — AdminBootstrap at startup from Admin:Email (+ Admin:Password to create); compose passes P2S_ADMIN_EMAIL/P2S_ADMIN_PASSWORD (empty by default). Test AdminAccount_IsBootstrappedFromConfiguration
- Task 26: Ruling: admin endpoints use IPlan2SpaceDbContext, never return password hashes, cap 500 rows, include owner email/project count; jobs merge live Redis status/error (DB rows alone stay "Queued" forever)
- Task 26: Ruling: login response has no role, so the Studio decodes sub/role from the JWT (display only; API still authorizes); /admin route + page (users/projects/jobs, "Admins only" on 403), Dashboard Admin link for admins
- Task 27: Ruling: brief's smoke script polled /api/health which didn't exist (the wait loop silently timed out and continued), read ["id"] from the upload response (API returns fileId), used localhost/python3 (IPv6/WSL + Windows Store stub here), POSTed exports without a body (400) and had no cleanup trap — added HealthController (anonymous, DB check, 503 when unreachable) with test HealthTests; script fixed accordingly
- Task 27: Ruling: brief ran `docker compose up/down` in the default project — locally that would tear down the developer's stack and clash with override ports. Smoke runs as project p2s-smoke on P2S_HTTP_PORT=8088 (nginx port now ${P2S_HTTP_PORT:-80}), -f docker-compose.yml only, --env-file .env.example, empty LLM keys, trap cleanup with logs on failure
- Task 27: Ruling: interface says "poll WebSocket to completion" — script polls the status API, then verifies the job WebSocket through nginx with ?access_token= using the ai image as a throwaway client on the public network (expects the final Completed/100 frame)
- Task 27: Ruling: first smoke run found a compose gap — api calls ai (co-pilot/staging/export) but didn't depend on it, so `up nginx` left /export at 503. ai got a /health healthcheck and api depends on ai healthy; api has a wget /api/health healthcheck and nginx waits for it
- Task 27: Ruling: CI — backend job starts compose PostGIS (SchemaTests use 127.0.0.1:5433), ai job installs libgl1/libglib2.0-0 for opencv, frontend job also runs `npm run build` (tsc). Workflow validated structurally only: the repo has no GitHub remote, so CI has NOT run
- Final: Ruling: reviewer I3 "worker retries once on 409 = stale overwrite" — kept: an import is meant to replace the plan and the client now confirms before replacing a non-empty/dirty plan — cost if wrong: an import racing a manual save in another tab replaces it
- Final: Ruling: I4 "no autosave (Task 21 title)" — solved data loss with dirty tracking + per-project localStorage draft + beforeunload instead of timed autosave (autosave would push half-finished drags as new versions and trigger conflicts in other tabs); refresh-token flow still deferred (Task 26) — cost if wrong: users must press Save
- Final: Ruling: my Task 26 "promote-only mode" ruling was wrong (reviewer): replaced by "configured password proves ownership" — cost if wrong: an operator who set only Admin:Email must also set Admin:Password
- Final: Ruling: reviewer's MLINE/ARC wall support not added — entity support beyond LINE/POLYLINE is new scope; the NoWallsFoundError message names the drawing's layers so users know why — cost if wrong: MLINE-only drawings fail with that message

## Final whole-branch review — fixes (7)

- Final: fixed C1 raster merge O(n^3) hang + no time limits — test_merging_thousands_of_segments_is_fast_and_correct, test_absurd_segment_counts_fail_fast_with_a_clear_message, test_vectorize_job_has_time_limits_and_late_acks, test_hitting_the_soft_time_limit_reports_a_readable_failure, test_text_and_dimension_labels_are_not_vectorized_as_walls RED→GREEN (merge speed test not run against the old O(n^3) code: reviewer measured it at days). Reviewer's 1600x1200/400-label plan now 0.1 s
- Final: fixed I2 job state only in Redis / stuck jobs never end — JobLifecycleTests (4) + test_final_job_state_is_written_to_the_api_database, test_final_state_reporting_never_breaks_the_job RED→GREEN
- Final: fixed I3 server-side actions ignoring/overwriting unsaved edits — StaleClientVersion_Returns409AndChangesNothing (.NET), unsavedEdits.test.tsx co-pilot/export/import cases RED→GREEN
- Final: fixed I4 edits lost / save errors hidden — unsavedEdits.test.tsx (dirty+draft, draft restore, stale draft dropped, beforeunload, save error shown) RED→GREEN
- Final: fixed I5 rooms never created — test_rooms.py (5) + RoomLayer.test.tsx RED→GREEN; 3D floors not added (deferred minor)
- Final: fixed I6 account hardening + admin bootstrap squatting — AccountHardeningTests (8) RED→GREEN
- Final: fixed I7 DXF double faces / unitless mm / 5 cm snap on CAD input — test_two_face_lines_become_one_centreline_wall_with_the_gap_as_thickness, test_project_sample_plan_faces_pair_into_six_walls_that_enclose_three_rooms, test_unitless_drawing_in_millimetres_is_detected, test_dxf_walls_are_healed_within_the_drawing_tolerance RED→GREEN

## Deferred minors (12)

- Wall/Opening toolbar tools are decorative — users can only move existing walls, not draw new ones
- SaveGeometry accepts thickness/height <= 0 or huge and all-identical wall points
- StudioPage swallows load failures; store not reset between projects (draft is per project)
- the WS access_token query string appears in nginx access logs
- nginx /ws/ has no proxy_read_timeout (quiet stages > 60 s drop the socket; client falls back to polling)
- worker mkdtemp dirs never removed; Redis job keys have no TTL; MinIO uploads/exports orphaned on project delete; each export stores a new object
- healing union-find can chain short tessellated segments into one point; O(n^2) on endpoints
- OpenAI client default 600 s timeout vs .NET 30 s; no co-pilot message length limit
- no test for the DbUpdateConcurrencyException race path; web conflict test is mock-only
- dead code — geometryStore.applyAiResult, loadFromServer maxVersion fallback
- celery_worker (parses untrusted files) holds MinIO root creds + service token; ai egress not allow-listed
- 3D viewer has no room floors
